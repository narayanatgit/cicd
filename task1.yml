platform: linux

image_resource:
  type: registry-image
  source:
    repository: python
    tag: "3.10"

inputs:
- name: my-repo

outputs:
- name: local-output   # 👈 THIS is key

run:
  path: bash
  args:
    - -exc
    - |
      set -eux
      cd ..
      pwd
      cd ..
      pwd
      ls
      cd mnt
      ls
