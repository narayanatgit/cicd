platform: linux

image_resource:
  type: registry-image
  source:
    repository: alpine

inputs:
- name: repo

params:
  DEVICE_IP:
  USERNAME:
  MESSAGE:

run:
  path: sh
  args:
  - -c
  - |
    set -e
    ls -l
    ls -l repo
    chmod +x repo/test.sh
    sh repo/test.sh
