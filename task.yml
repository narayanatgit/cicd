platform: linux

image_resource:
  type: registry-image
  source:
    repository: alpine
    tag: "3.19"

inputs:
- name: repo

params:
  DEVICE_IP:
  USERNAME:
  MESSAGE:

run:
  path: sh
  args:
  - -c
  - |
    set -e
    chmod +x repo/test.sh
    sh repo/test.sh
