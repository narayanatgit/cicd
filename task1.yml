platform: linux

image_resource:
  type: registry-image
  source:
    repository: python
    tag: "3.10"

inputs:
- name: my-repo

outputs:
- name: my-repo   # 👈 THIS is key

run:
  path: python
  args:
    - -c
    - |
      with open("my-repo/myfile.txt", "w") as f:
          f.write("Hello from Concourse using PUT!\n")
       
    
